# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out


.lock 
*.pem
**/__debug_bin
cmd/airlyctl/airlyctl
cmd/krkstopsctl/krkstopsctl
cmd/ttssctl/ttssctl
cmd/stopsctl/stopsctl
cmd/krkstops/krkstops
cmd/mock/mock
cmd/ttssmonitord/ttssmonitord
.env.priv
*.ign